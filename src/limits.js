'use strict';

// Keep generated attachments below Discord's lowest commonly supported upload limit.
const MAX_DISCORD_ATTACHMENT_BYTES = 7 * 1024 * 1024;

module.exports = { MAX_DISCORD_ATTACHMENT_BYTES };
